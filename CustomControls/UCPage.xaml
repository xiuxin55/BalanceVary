<UserControl x:Class="CustomControls.UCPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="217*"/>
            <ColumnDefinition Width="83*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Column="0">
            <Button Content="首页"></Button>
            <Button Content="上一页" Margin="5,0,0,0"></Button>
            <TextBox Width="40" TextAlignment="Center"  Margin="5,0,0,0"></TextBox>
            <Button Content="下一页"  Margin="5,0,0,0"></Button>
            <Button Content="尾页"  Margin="5,0,0,0"></Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
            <TextBlock Text="共 "/>
            <TextBlock Text="{Binding Total}"/>
            <TextBlock Text="  页"/>
            <TextBlock Text="/共 "/>
            <TextBlock Text="{Binding TotalItem}"/>
            <TextBlock Text="  条"/>
        </StackPanel>
    </Grid>
</UserControl>
